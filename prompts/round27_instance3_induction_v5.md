# Round 27 (Instance 3): Induction Pack v5 Under Corrected Bookkeeping

Use only your prior outputs plus this prompt. Do not claim fresh scans.

## Locked context

- k=1 micro-lemma remains robust.
- Step profile remains: step2 dominates X<0; step3 exceptions are only at k=1.
- Old bookkeeping form is false; calibrated/extended forms must be used.

## Task

Write a near-final induction framework with three branches:
1. k=1 branch via micro-lemma,
2. k>=2,t=2 branch via corrected reserve inequality,
3. k>=2,t>=3 bridge branch.

Requirements:
1. Use only bookkeeping forms consistent with Round 27 Instance 1/2 output.
2. Give two variants if needed: (A) calibrated-alpha, (B) split-reserve.
3. Provide a non-circular dependency graph.
4. State exactly one unresolved bottleneck per variant.

## Output format

1. `Variant A induction theorem`
2. `Variant B induction theorem`
3. `Dependency graph`
4. `Unresolved bottleneck(s)`
5. `Recommended manuscript default`
