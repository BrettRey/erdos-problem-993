# Round 31 (Instance 3): Induction Pack v9 After Envelope Break

Use only your prior outputs plus this prompt. Do not claim fresh scans.

## Locked context

- k=1 micro-lemma branch remains robust and independent.
- v8 k>=2 closure used margin `alpha(n)-lambda(n)`.
- n=22 now has negative global margin under current envelope (`alpha<lambda`), so v8 cannot close globally as stated.

## Task

Write an induction v9 architecture that remains non-circular after global envelope break:

1. Preserve three-branch structure (`k=1`, hard step `t=2`, bridge `t>=3`).
2. Replace the broken single-envelope k>=2 branch with a repaired branch schema (e.g., class-stratified envelope, corrected reserve family, or symmetric extra-term cancellation).
3. Keep unresolved interfaces minimal and explicit.
4. State exactly what must be proved at `t=2` versus what can be deferred to the bridge.
5. Include a failure escalation clause if the repaired branch also drifts.

## Output format

1. `Induction theorem v9 (post-break)`
2. `k>=2 repaired branch schema`
3. `Dependency graph (non-circular, minimal interfaces)`
4. `Hard-step vs bridge proof obligations`
5. `Escalation path if repaired margin drifts`

