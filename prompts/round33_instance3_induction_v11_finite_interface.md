# Round 33 (Instance 3): Induction v11 with Smallest Finite Hard-Step Interface

Use only your prior outputs plus this prompt. Do not claim fresh scans.

## Locked context

- Branch `k=1` remains closed by the micro-lemma.
- v10 uses class-stratified channel-vector sandwich at hard step `t=2`.
- n=23 break is internal to the `a=2` line (`(2,19)` vs `(2,18)`).

## Task

Compress v10 into the smallest practical finite proof interface:

1. Propose a concrete hard-step class partition that is just rich enough to separate the n=23 conflict.
2. State a v11 theorem with explicit class-indexed lower/upper faces and coordinatewise channel margins.
3. Isolate exactly what is unresolved at `t=2` vs what can remain deferred at bridge `t>=3`.
4. Provide a non-circular dependency graph in final theorem style.
5. Add a drift escalation clause that explains how to refine partition/reserve if n>=24 adds a new internal conflict.

## Output format

1. `Induction theorem v11 (finite-interface form)`
2. `Hard-step finite class partition`
3. `Unresolved obligations (hard vs bridge)`
4. `Dependency graph (non-circular)`
5. `Escalation rule under further drift`
