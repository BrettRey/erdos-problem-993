# Round 35 Prompt Prep (2026-03-03)

Goal: convert Round 34 outputs into canonical, implementation-ready interfaces with deterministic certificates and minimal unresolved lemmas.

## Inputs received (Round 34, 4 outputs)

1. Regime-adaptive repaired theorem with partition algorithm `Pi(n)` (refine-on-obstruction) and bucketwise closure/falsification.
2. Conditional `a=2` local module with activation/deactivation, exact `(rho,lambda)` feasibility, composition ledger, and runtime acceptance checks.
3. Induction v12 regime-switched hard-step interface with finite class template and deterministic escalation.
4. Orchestrator v10 with full schema, state machine, scoring, n=24 queue template, and stop checks.

## Locked computational context for prompt anchoring

Use only completed n<=24 frontier data as authoritative:

- `alpha_front(24)=0.16161242603550297` at class `(2,20)`
- `lambda_front(24)=0.280781720999777` at class `(4,18)`
- gap `=-0.11916929496427403`

n=25 remains partial/stalled and should be treated as non-authoritative.

## Round 35 objectives

1. Canonicalize `Pi(n)` as a deterministic finite algorithm with explicit termination/correctness and minimality certificates.
2. Reduce the `a=2` local module to a solver-ready 1D/2D optimization interface with explicit infeasibility certificates and composition guarantees.
3. Integrate v12 into a single v13 theorem interface directly consumable by orchestrator output (`ENV/A2_LOCAL/HARDSTEP` bucket obligations).
4. Turn orchestrator v10 into an execution spec with deterministic pseudocode, reproducible scoring/tie-break behavior, and partial-data handling.

## Prompt design constraints

- Keep all-diagonal framework and boundary-correct prefix regime fixed.
- Use only locked constants and existing objects; no fresh scan claims.
- Ask for finite interfaces, explicit certificates, and deterministic tie-breaks.
- Maintain 5-section output format for each instance prompt.
