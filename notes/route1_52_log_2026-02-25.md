# Route-1 / 5.2 Log (2026-02-25)

## Items worth preserving

1. Provenance note on analogy usage
- The Nature surface-minimization paper was useful as an analogy pump ("global objective + local motif calculus + exact mapping"), but it is not a theorem source for this project's combinatorial claims.
- The actionable result came from in-project algebra (full-jet inversion), not from importing external physical-network theorems.

2. Closed/falsified routes
- K1 one-cell localization route is closed: canonical witness with `Delta/B = 148/99 > 1` shows K1+L2 does not force `N`.
- K2 one-cell variance localization route is closed: canonical 7-vertex witness at `lambda = 7/15` has `Var_ext - Var > S(lambda)`.
- Any proof using `kappa_var = |d-m|` is invalid (counterexample `g6='DQo'`: `d=1`, `m=2`, actual `kappa_var=0`).

3. Fixed-lambda collisions do not auto-lift to canonical K2
- Verified explicit pair with same canonical triplet and same `P`, but different `Q`, hence different derived lambda:
  - `T_end = HkCGGC@`, `T_2 = HgcGGC@`
  - Both have `P = [1,6,10,4]`, `m=3`
  - Derived `lambda`: `4/5` vs `7/9`
- Therefore external fixed-lambda forest-side collisions do not imply canonical K2 collisions.

4. Positive algebraic result to keep
- Full-jet inversion (degree `d`) is exact:

```text
N = (1/lambda) *
    (sum_{r=1}^d (-1)^(r-1) mu_r/(r-1)!) /
    (1 + sum_{r=1}^d (-1)^r mu_r/r!),
mu_r = lambda^r P^{(r)}(lambda)/P(lambda).
```

- This is a valid `K_full -> N` mapping; it does not imply `K1` or `K2` identifiability.

5. Current bottleneck / next theorem target
- Determine smallest finite truncation `r` such that canonical `K_r = (d,m,lambda,mu_1,...,mu_r)` forces `N`.
- Operationally: search for first canonical split at fixed `K_r` (if any) or prove injectivity for that `r` on canonical image.

## Context status
- Through current scans up to `n<=23`: no observed canonical split for K1 or K2, but this remains empirical.
- Treat "no split observed" as evidence only, not closure.
